#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, k;
        cin >> n >> k;
        int c = 0;
        if (t <= 10 && n <= 100)
        {
            int a[n];
            for (int i = 0; i < n; i++)
            {
                cin >> a[i];
            }
            // for (int i = 0; i < n; i++)
            {
                cout << c << endl;
            }
        }
    }

    return 0;
}
